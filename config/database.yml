default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: kindling_dev
  host: localhost
  username: matt
  password: <%= Rails.application.credentials.db_password_dev %>

test:
  <<: *default
  database: kindling_test
  host: localhost
  username: matt
  password: <%= Rails.application.credentials.db_password_dev %>
